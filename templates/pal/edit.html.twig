{% extends 'admin.base.html.twig' %}

{% block title %}ToolKit - PAL bearbeiten{% endblock %}

{% block body %}
    <main class="content">
        <div class="container-fluid p-0">

            <div class="mb-3">
                <h1 class="h3 d-inline align-middle">PAL bearbeiten</h1>
            </div>

            <div class="row">
                <div class="col-12 col-xl-12">
                    <div class="card">
                        <div class="card-header">
                            <h5 class="card-title">PAL bearbeiten</h5>

                        </div>
                        <div class="card-body">
                            {{ include('pal/_form.html.twig', {'button_label': 'Update'}) }}

                            <a href="{{ path('pal_index') }}">Zurück zur Übersicht</a>

                            {{ include('pal/_delete_form.html.twig') }}

                        </div>
                    </div>
                </div>
            </div>

        </div>
    </main>

{% endblock %}

