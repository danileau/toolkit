{% extends 'admin.base.html.twig' %}

{% block title %}ToolKit - Neues Gewicht{% endblock %}

{% block body %}
    <main class="content">
        <div class="container-fluid p-0">

            <div class="mb-3">
                <h1 class="h3 d-inline align-middle">Gewicht hinzufügen</h1>
            </div>

            <div class="row">
                <div class="col-12 col-xl-12">
                    <div class="card">
                        <div class="card-header">
                            <h5 class="card-title">Neues Gewicht</h5>
                            <h6>Bitte nur den Gewichtswert eingeben <b>OHNE</b> den Zusatz "kg"</h6>
                        </div>
                        <div class="card-body">
                            {{ include('gewicht/_form.html.twig', {'button_label': 'Erstellen'}) }}

                        </div>
                    </div>
                </div>
            </div>

        </div>
    </main>

{% endblock %}
    {% block js %}
        <script src="{{ asset('js/app.js')}}"></script>
        <script src="{{ asset('js/datatables.js') }}"></script>

        <script>
          document.addEventListener("DOMContentLoaded", function() {
            // Datatables Responsive
            $("#datatables-reponsive").DataTable({
              responsive: true
            });
          });
        </script>

    {% endblock %}
