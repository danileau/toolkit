<form method="post" action="{{ path('calipometrie_delete', {'id': calipometrie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ calipometrie.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
